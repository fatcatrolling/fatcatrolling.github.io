# Planning / source files — never deployed
Work History_for AI.csv
Brief.txt

# Bug recordings + asset sources — kept locally, not shipped
bugs/
images/OpenGraph/

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.vscode/
.gstack/

# Claude Code local config — never published
.claude/

# Design reference images (color authoring sources, not site assets)
images/theme_*
images/colorPalette_*

# Internal-only docs and dev tools (kept local, not on GitHub)
CLAUDE.md
DNS-SETUP.md
README.md
validate.html
